---
---

<button
  type="button"
  id="theme-selector"
  class="px-4 py-2 rounded bg-buttonBg dark:bg-buttonBgDark text-textMain dark:text-textMainDark hover:bg-accent dark:hover:bg-accentDark transition-colors"
>
  Toggle Theme
</button>

<script>
  document.getElementById('theme-selector')?.addEventListener('click', () => {
    if (localStorage.theme === 'dark') {
      localStorage.theme = 'light';
      document.documentElement.classList.remove('dark');
    } else {
      localStorage.theme = 'dark';
      document.documentElement.classList.add('dark');
    }
  });
</script>
